---
layout: single
title: "창 띄우기"
---

# OpenGL 창 띄우기 예제

아래는 GLFW와 GLAD를 사용하여 간단한 OpenGL 창을 띄우는 C++ 코드입니다.

```cpp
#include<iostream>
#include<glad/glad.h>
#include<GLFW/glfw3.h>

int main(){
    glfwInit();
    glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 3);
    glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 3);
    glfwWindowHint(GLFW_OPENGL_PROFILE,GLFW_OPENGL_CORE_PROFILE);

    GLFWwindow* window = glfwCreateWindow(800, 800, "YoutubeOpenGL", NULL, NULL);
    if (window == NULL) {
        std::cout << "GLFW window failed" << std::endl;
        glfwTerminate();
        return -1;
    }
    glfwMakeContextCurrent(window);

    gladLoadGL();

    glViewport(0, 0, 800, 800);

    glClearColor(0.07f, 0.13f, 0.17f, 1.0f);
    glClear(GL_COLOR_BUFFER_BIT);
    glfwSwapBuffers(window);

    while (!glfwWindowShouldClose(window)) {
        glfwPollEvents();
    }

    glfwDestroyWindow(window);
    glfwTerminate();
    return 0;
}

---

### **3. 주요 변경 사항**
1. 코드 블록을 ```cpp로 감싸 언어를 명시했습니다.
   - 이렇게 하면 Markdown 렌더러에서 C++ 문법 강조를 자동으로 적용합니다.
2. 코드와 본문을 구분하기 위해 적절한 설명을 추가했습니다.
3. YAML 프론트매터 아래에 간단한 설명을 덧붙여 내용을 명확히 했습니다.

---

### **4. 출력 결과**
Markdown 렌더링 시, 위 코드 블록은 C++로 문법 강조가 적용된 상태로 표시됩니다. 
예를 들어, 아래처럼 렌더링됩니다:

```cpp
#include<iostream>
#include<glad/glad.h>
#include<GLFW/glfw3.h>

int main(){
    // Your code here...
}

